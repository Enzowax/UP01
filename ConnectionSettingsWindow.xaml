<Window x:Class="ShutIKrol.ConnectionSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройка подключения к БД"
        Width="460" Height="400"
        Background="#1E1E2E"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,4"/>
        </Style>
        <Style TargetType="Button" x:Key="Btn">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                   Text="🔌 Подключение к SQL Server"
                   Foreground="#CDD6F4" FontSize="18" FontWeight="Bold"
                   Margin="0,0,0,16"/>

        <!-- Server -->
        <Label Grid.Row="1" Grid.Column="0" Content="Сервер:"/>
        <ComboBox Grid.Row="1" Grid.Column="1" x:Name="ServerBox" IsEditable="True"
                  Margin="0,4">
            <ComboBoxItem Content="." IsSelected="True"/>
            <ComboBoxItem Content="(local)"/>
            <ComboBoxItem Content=".\SQLEXPRESS"/>
            <ComboBoxItem Content="(local)\SQLEXPRESS"/>
            <ComboBoxItem Content="(localdb)\MSSQLLocalDB"/>
            <ComboBoxItem Content="(localdb)\ProjectsV13"/>
        </ComboBox>

        <!-- Database -->
        <Label Grid.Row="2" Grid.Column="0" Content="База данных:"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="DatabaseBox"
                 Text="UP_ShutIKrol" Margin="0,4"/>

        <!-- Auth mode -->
        <Label Grid.Row="3" Grid.Column="0" Content="Аутентификация:" VerticalAlignment="Top" Margin="0,8,0,0"/>
        <StackPanel Grid.Row="3" Grid.Column="1" Margin="0,4">
            <RadioButton x:Name="WinAuthBtn" Content="Windows Authentication (текущий пользователь)"
                         IsChecked="True" Checked="AuthMode_Changed"/>
            <RadioButton x:Name="SqlAuthBtn" Content="SQL Server Authentication (логин/пароль)"
                         Checked="AuthMode_Changed"/>
        </StackPanel>

        <!-- Login -->
        <Label Grid.Row="4" Grid.Column="0" Content="Логин SQL:" x:Name="LoginLabel"/>
        <TextBox Grid.Row="4" Grid.Column="1" x:Name="LoginBox" Margin="0,4"/>

        <!-- Password -->
        <Label Grid.Row="5" Grid.Column="0" Content="Пароль SQL:" x:Name="PasswordLabel"/>
        <PasswordBox Grid.Row="5" Grid.Column="1" x:Name="PasswordBox"
                     Background="#313244" Foreground="#CDD6F4"
                     BorderBrush="#45475A" Padding="6,4" FontSize="13"
                     Margin="0,4"/>

        <!-- Status -->
        <TextBlock Grid.Row="6" Grid.ColumnSpan="2" x:Name="StatusText"
                   Foreground="#F38BA8" FontSize="12"
                   TextWrapping="Wrap" VerticalAlignment="Bottom" Margin="0,8,0,0"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="🔍 Проверить" Background="#89B4FA" Foreground="#1E1E2E"
                    Style="{StaticResource Btn}" Click="Test_Click" Margin="0,0,8,0"/>
            <Button Content="💾 Сохранить и продолжить" Background="#A6E3A1" Foreground="#1E1E2E"
                    Style="{StaticResource Btn}" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
